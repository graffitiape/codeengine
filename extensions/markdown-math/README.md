# Markdown Math

**Notice:** This extension is bundled with Code Engine. It can be disabled but not uninstalled.

Adds math rendering using [KaTeX](https://katex.org) to Code Engine's built-in markdown preview and markdown cells in notebooks.
